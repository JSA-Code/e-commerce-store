import { Josefin_Sans, Rubik } from 'next/font/google';

export const primary = Josefin_Sans({
  subsets: ['latin'],
  variable: '--font-josefin-sans',
});

export const secondary = Rubik({
  subsets: ['latin'],
  variable: '--font-rubik',
});
